---
phase: 01-audit-gap-analysis
plan: 02
type: execute
---

<objective>
Scan the main RenderDraw package for all LMS message channel definitions and all Aura event definitions, extracting their full metadata (fields, attributes, types).

Purpose: Establish the complete source-of-truth inventory for LMS channels and Aura events — the two messaging/eventing API surfaces that customers interact with.
Output: `source-lms-events.md` in this phase directory — structured inventory of all channels and events with their fields.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-gap-analysis/01-RESEARCH.md

**Source paths:**
- LMS channels: `/Users/erikpilgrim/Documents/Dev/Personal/RenderDraw SFDC/RenderDraw/force-app/main/default/messageChannels/`
- Aura events: `/Users/erikpilgrim/Documents/Dev/Personal/RenderDraw SFDC/RenderDraw/force-app/main/default/aura/` (*.evt files)

**From research:**
- 5 LMS message channels
- 56 Aura events (.evt files)
- 22 Settings_* events may be internal-only — classify but don't exclude yet
- Audit patterns: grep for `<lightningMessageFields>`, `aura:attribute` in .evt, `fireEvent`/`fire(` for origins
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all LMS message channel definitions with fields</name>
  <files>.planning/phases/01-audit-gap-analysis/source-lms-events.md</files>
  <action>
Scan all `.messageChannel-meta.xml` files in the main RenderDraw package messageChannels directory.

For each channel:
1. Read the full XML file
2. Extract: channel name (from filename and `<masterLabel>`), description (if present), all `<lightningMessageFields>` with their `<fieldName>` and `<description>`
3. Search the entire force-app directory for components that publish to this channel (`publish(` + channel import reference) and subscribe (`subscribe(` + channel import reference) to identify origin and consumer components

Write to `source-lms-events.md`:

# Source Inventory: LMS Channels & Aura Events

## LMS Message Channels (5)

| Channel | Master Label | Fields | Publishers | Subscribers |
|---------|-------------|--------|------------|-------------|
| Canvas_Interaction | ... | field1, field2, ... | component1, component2 | component3, component4 |
...

### Channel Detail

#### Canvas_Interaction
- **File:** `messageChannels/Canvas_Interaction.messageChannel-meta.xml`
- **Fields:**
  | Field Name | Description |
  |------------|-------------|
  ...
- **Published by:** [list components with file paths]
- **Subscribed by:** [list components with file paths]

[Repeat for each channel]

**Grep patterns for publisher/subscriber discovery:**
- LWC: `import.*from '@salesforce/messageChannel/[ChannelName]__c'` then check for `publish(` vs `subscribe(`
- Aura: `lightning:messageChannel` tag with `type="[ChannelName]__c"` and `onMessage` (subscriber) vs code calling `.publish()` (publisher)
  </action>
  <verify>
- source-lms-events.md exists with LMS section
- Exactly 5 channels listed (matches research count)
- Each channel has fields extracted from XML
- Publisher/subscriber lists are populated (at least publishers — some channels may be publish-only from RenderDraw's perspective)
  </verify>
  <done>All 5 LMS channels inventoried with field definitions, publishers, and subscribers.</done>
</task>

<task type="auto">
  <name>Task 2: Extract all Aura event definitions with attributes</name>
  <files>.planning/phases/01-audit-gap-analysis/source-lms-events.md (append)</files>
  <action>
Scan all `.evt` files in the main RenderDraw package aura directory.

For each event:
1. Read the .evt file to extract: event name, type (APPLICATION or COMPONENT from `<aura:event type="..."`), all `<aura:attribute>` tags with name, type, and description
2. Classify into categories based on naming prefix:
   - `EVT_Renderer_*` — Renderer events
   - `EVT_Settings_*` — Settings events (flag as "likely internal")
   - `EVT_Admin*` — Admin events
   - `EVT_AdvancedLayout_*` — Layout events
   - `EVT_Scene*` — Scene events
   - `EVT_InteractionEvent_*` — Interaction events
   - Other

Append to `source-lms-events.md`:

## Aura Events (56)

### Summary by Category
| Category | Count | Likely Public | Likely Internal |
|----------|-------|---------------|-----------------|
| Renderer | N | Y | Z |
| Settings | N | Y | Z |
...

### Event Inventory
| Event Name | Type (App/Comp) | Category | Attributes | Likely Scope |
|------------|-----------------|----------|------------|--------------|
| EVT_Renderer_Loaded | APPLICATION | Renderer | attr1:String, attr2:Object | Public |
...

### Event Detail (grouped by category)

#### Renderer Events (N)

##### EVT_Renderer_Loaded
- **File:** `aura/EVT_Renderer_Loaded/EVT_Renderer_Loaded.evt`
- **Type:** APPLICATION
- **Attributes:**
  | Name | Type | Description |
  |------|------|-------------|
  ...

[Repeat for each event]

**Classification heuristic for Likely Scope:**
- APPLICATION events = more likely public (cross-component communication)
- COMPONENT events used only within Settings family = likely internal
- Events with `access="global"` in their definition = definitely public
- Default to "public" unless clearly internal — Phase 3 will do deeper analysis
  </action>
  <verify>
- Aura Events section added to source-lms-events.md
- Exactly 56 events listed (matches research count)
- Each event has type (APPLICATION/COMPONENT) extracted
- Categories assigned based on prefix
- Attributes extracted from .evt XML for each event
- Summary table counts sum to 56
  </verify>
  <done>All 56 Aura events inventoried with type, category, attributes, and scope classification. source-lms-events.md is the complete LMS + events source inventory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] source-lms-events.md exists with both LMS and Events sections
- [ ] 5 LMS channels with field details
- [ ] 56 Aura events with attribute details
- [ ] Events categorized by prefix and classified for likely scope
- [ ] Publisher/subscriber info populated for LMS channels
</verification>

<success_criteria>
- Complete LMS channel inventory with fields and origin/consumer components
- Complete Aura event inventory with attributes, types, and scope classification
- source-lms-events.md is self-contained and usable by Plan 01-05
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-gap-analysis/01-02-SUMMARY.md`
</output>
