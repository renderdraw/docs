---
phase: 01-audit-gap-analysis
plan: 03
type: execute
---

<objective>
Scan all LWC and Aura components in the main RenderDraw package, extracting their public API surfaces (@api properties for LWC, aura:attribute for Aura).

Purpose: Build the complete component API inventory — the largest surface area (163 LWC + 35 Aura = 198 components) that later phases will document.
Output: `source-components.md` in this phase directory — per-component inventory of all public properties/attributes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-gap-analysis/01-RESEARCH.md

**Source paths:**
- LWC: `/Users/erikpilgrim/Documents/Dev/Personal/RenderDraw SFDC/RenderDraw/force-app/main/default/lwc/`
- Aura: `/Users/erikpilgrim/Documents/Dev/Personal/RenderDraw SFDC/RenderDraw/force-app/main/default/aura/`

**From research:**
- 163 LWC components, 35 Aura components
- Priority LWC components with likely @api surfaces: canvas2D, canvas3D, layout, droppableArea, draggableItem, interactiveMap, virtualTour, cadConversion, pdfEditor, annotateIt, annotations3D, dynamicContentComponent_* family
- Extracted Canvas2D/3D docs already cover canvas2D (24 @api) and canvas3D (25 @api) — note existence but still scan source for current state
- Grep pattern: `@api` in .js files for LWC, `aura:attribute` in .cmp files for Aura
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan all LWC components for @api properties</name>
  <files>.planning/phases/01-audit-gap-analysis/source-components.md</files>
  <action>
Scan all 163 LWC component directories in the main RenderDraw package.

For each LWC component:
1. Find the main .js file in the component directory
2. Grep for `@api` declarations — extract property name and any JSDoc type annotation above it
3. Also check for `@api` methods (public methods exposed to parent components)
4. Count total @api items per component

**Efficient approach:** First, run a batch grep across all LWC .js files to identify which components HAVE @api properties:
```bash
grep -rln "@api" --include="*.js" [lwc-path]
```
Then only deep-read components that have @api. Components with zero @api are listed but not detailed.

Write to `source-components.md`:

# Source Inventory: Component APIs

## LWC Components (163 total)

### Summary
| Has @api | No @api | Total |
|----------|---------|-------|
| N | M | 163 |

### Components with @api Properties
| Component | @api Properties | @api Methods | Total @api |
|-----------|----------------|--------------|------------|
| canvas2D | prop1, prop2, ... | method1, method2 | 24 |
| canvas3D | prop1, prop2, ... | method1, method2 | 25 |
...

### Component Detail (only components with @api)

#### canvas2D
- **Path:** `lwc/canvas2D/canvas2D.js`
- **@api Properties:**
  | Property | Type (from JSDoc/default) |
  |----------|--------------------------|
  ...
- **@api Methods:**
  | Method | Parameters |
  |--------|-----------|
  ...

[Repeat for each component with @api]

### Components without @api (list only)
componentA, componentB, componentC, ...

**Note:** For canvas2D and canvas3D, the extracted API docs (Canvas2D_API_Documentation.md, Canvas3D_API_Documentation.md) have comprehensive documentation. Flag these as "extracted docs available" in the inventory.
  </action>
  <verify>
- source-components.md exists with LWC section
- Total component count is 163 (or close — some may be utility modules without .js)
- canvas2D shows ~24 @api properties (matches extracted doc count)
- canvas3D shows ~25 @api properties (matches extracted doc count)
- Components without @api are listed but not detailed
  </verify>
  <done>All 163 LWC components scanned. Components with @api have per-property detail. Components without @api are listed.</done>
</task>

<task type="auto">
  <name>Task 2: Scan all Aura components for public attributes</name>
  <files>.planning/phases/01-audit-gap-analysis/source-components.md (append)</files>
  <action>
Scan all 35 Aura component directories in the main RenderDraw package. Only scan .cmp files (not .evt files — those are covered in Plan 01-02).

For each Aura component (.cmp file):
1. Extract all `<aura:attribute>` tags with: name, type, default (if present), description (if present), access (if present — `global` vs `public` vs default)
2. Note if the component has `access="global"` on the `<aura:component>` tag itself (making it available outside the package)
3. Count total public attributes per component

**Efficient approach:** First batch grep to find which components have attributes:
```bash
grep -rln "aura:attribute" --include="*.cmp" [aura-path]
```

Append to `source-components.md`:

## Aura Components (35 total)

### Summary
| Has Attributes | No Attributes | Global Access | Total |
|---------------|---------------|---------------|-------|
| N | M | G | 35 |

### Components with Public Attributes
| Component | Attributes | Global Access | Total Attrs |
|-----------|-----------|---------------|-------------|
| AdvancedRenderer | attr1, attr2, ... | Yes/No | N |
...

### Component Detail (only components with attributes)

#### AdvancedRenderer
- **Path:** `aura/AdvancedRenderer/AdvancedRenderer.cmp`
- **Component Access:** global / public
- **Attributes:**
  | Name | Type | Default | Access | Description |
  |------|------|---------|--------|-------------|
  ...

[Repeat for each component with attributes]

### Components without Public Attributes (list only)
componentA, componentB, ...

**Important:** Exclude .evt directories from this scan — only look at directories containing .cmp files. Many aura/ subdirectories are event definitions (EVT_*), not components.
  </action>
  <verify>
- Aura Components section added to source-components.md
- ~35 Aura components listed (excluding EVT_* directories — actual .cmp count may differ from total aura/ subdirectory count)
- Each component with attributes has per-attribute detail including type and access level
- Global access components are flagged
  </verify>
  <done>All Aura components scanned. Components with attributes have per-attribute detail. source-components.md is the complete component API inventory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] source-components.md exists with both LWC and Aura sections
- [ ] LWC summary counts sum correctly
- [ ] Aura summary counts sum correctly
- [ ] Priority components (canvas2D, canvas3D, AdvancedRenderer) have detailed property listings
- [ ] canvas2D/canvas3D flagged as having extracted docs available
</verification>

<success_criteria>
- Complete LWC component inventory with per-component @api property lists
- Complete Aura component inventory with per-component attribute lists
- source-components.md is self-contained and usable by Plan 01-05
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-gap-analysis/01-03-SUMMARY.md`
</output>
