---
phase: 03-aura-events
plan: 02
type: execute
domain: documentation
---

<objective>
Enrich the 9 existing Aura event pages under `api-documentation/3d-components-api/events-aura/` with Source Component / Handler Component / Event Name (for handler) sections, add missing attributes on pages that drifted from source, and populate the empty `evt_renderer_mesh_selection_cleared.md` stub.

Purpose: 9 Aura event pages already exist in GitBook but carry no information about which components fire or handle each event — developers cannot wire them up without reading source. Three pages have stale parameter tables (missing the `transaction` attribute that was added to Element_Added and Get_Hierarchy; missing source attributes on Mesh_Selected). One page (`evt_renderer_mesh_selection_cleared.md`) is an empty stub with only a heading. This plan fixes all three classes of gap in a single pass.

Output: 9 enriched event pages (8 appended with new sections, 1 populated from stub) plus a Phase 2–style Note block on no pages this round (the `isIsolated` commented-out attribute belongs to `EVT_Renderer_Element_Verify`, which is undocumented and handled by Plan 03-03).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source inventories (authoritative):
@.planning/phases/01-audit-gap-analysis/source-lms-events.md
@.planning/phases/03-aura-events/source-aura-event-mapping.md

# Gap report (identifies which pages need updates vs verification):
@.planning/phases/01-audit-gap-analysis/GAP-REPORT.md

# Phase 2 precedent — Publisher/Subscriber pattern to mirror as Source/Handler:
@.planning/phases/02-lms-message-channels/02-01-SUMMARY.md

# Plan 03-01 summary (produces the Source/Handler mapping this plan consumes):
@.planning/phases/03-aura-events/03-01-SUMMARY.md

# Existing Aura event pages to enrich (8 with content + 1 empty stub):
@api-documentation/3d-components-api/events-aura/README.md
@api-documentation/3d-components-api/events-aura/evt_renderer_context_details_closed.md
@api-documentation/3d-components-api/events-aura/evt_renderer_element_added.md
@api-documentation/3d-components-api/events-aura/evt_renderer_get_camerapositionandtarget.md
@api-documentation/3d-components-api/events-aura/evt_renderer_get_hierarchy.md
@api-documentation/3d-components-api/events-aura/evt_renderer_loaded.md
@api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selected.md
@api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md
@api-documentation/3d-components-api/events-aura/evt_renderer_screenshot_taken.md
@api-documentation/3d-components-api/events-aura/evt_renderer_select_component.md

**Key facts driving this plan:**

- **9 existing pages** under `api-documentation/3d-components-api/events-aura/`:
  1. `evt_renderer_context_details_closed.md` — content complete, needs new sections only
  2. `evt_renderer_element_added.md` — content mostly complete; **missing `transaction` attribute** per GAP-REPORT
  3. `evt_renderer_get_camerapositionandtarget.md` — content complete, needs new sections only
  4. `evt_renderer_get_hierarchy.md` — content mostly complete; **missing `transaction` attribute** per GAP-REPORT
  5. `evt_renderer_loaded.md` — content complete, needs new sections only
  6. `evt_renderer_mesh_selected.md` — content complete; **verify source has `name` attr matches doc**
  7. `evt_renderer_mesh_selection_cleared.md` — **empty stub**, needs full population
  8. `evt_renderer_screenshot_taken.md` — content complete but sparse (no Example Usage — leave as-is, do not fabricate)
  9. `evt_renderer_select_component.md` — content complete, needs new sections only

- Page structure convention (observed across existing pages):
  ```
  # EVT\_Name

  ### RDraw:EVT\_Name

  #### Usage Notes

  [prose description]

  #### Parameters

  [markdown table or "no parameters" prose]

  #### Example Usage

  Component

  [fenced code block — Aura markup]

  Controller

  [fenced code block — JS]
  ```

- The headings use `####` for sections within the page (not `###`). Namespace-prefixed anchor heading uses `###`. Top heading uses `#`.

**Constraining decisions (from PROJECT.md and Phase 2 precedent):**

- Public API surface only — all 9 pages qualify (all are Renderer events, most are `access="global"`).
- No restructuring — edit pages in place at `api-documentation/3d-components-api/events-aura/`. Do NOT touch root `SUMMARY.md` (page entries already exist there).
- Match existing voice — the pages use `####` subheadings and narrative prose. Append new sections; do NOT reorganize.

**Pattern to establish (new sections — mirrors Phase 2 LMS pattern adapted for Aura):**

Append the following three sections **after Parameters** and **before Example Usage** on every page:

```markdown
#### Source Component

* ComponentA
* ComponentB

#### Handler Component

* ComponentC
* ComponentD

*(If none in main package:)*
* None in main package — consumed by external implementing components.

#### Event Name (for handler)

\`\`\`xml
<aura:handler event="RDraw:EVT_Name" action="{!c.handleName}" />
\`\`\`
```

The empty stub `evt_renderer_mesh_selection_cleared.md` gets the full page structure (Usage Notes, Parameters, Source Component, Handler Component, Event Name for handler, Example Usage) populated from source.

**Attribute corrections (from GAP-REPORT):**

- `evt_renderer_element_added.md` — source has `transaction:String` attribute (non-required) not in docs table. Add it.
- `evt_renderer_get_hierarchy.md` — source has `transaction:String` attribute (non-required) not in docs table. Add it.
- `evt_renderer_mesh_selected.md` — verify the four attributes (context, id, name, uniqueId) all match source types. Source shows all four with `access="global"` and `required="true"` for name/id/uniqueId; `context` is optional. The doc table currently has `required` column mostly blank — update required cells for name/id/uniqueId to `Yes`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich 8 existing Renderer event pages with Source/Handler/Event Name sections (and attribute corrections on 3 pages)</name>
  <files>
    api-documentation/3d-components-api/events-aura/evt_renderer_context_details_closed.md
    api-documentation/3d-components-api/events-aura/evt_renderer_element_added.md
    api-documentation/3d-components-api/events-aura/evt_renderer_get_camerapositionandtarget.md
    api-documentation/3d-components-api/events-aura/evt_renderer_get_hierarchy.md
    api-documentation/3d-components-api/events-aura/evt_renderer_loaded.md
    api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selected.md
    api-documentation/3d-components-api/events-aura/evt_renderer_screenshot_taken.md
    api-documentation/3d-components-api/events-aura/evt_renderer_select_component.md
  </files>
  <action>
  For each of the 8 existing pages, inject three new sections **after `#### Parameters`** (and its table/prose) and **before `#### Example Usage`** (or at the end of the file if no Example Usage exists, as for `evt_renderer_screenshot_taken.md`).

  Insert in this order, using `####` heading level to match existing structure:

  1. `#### Source Component` — bulleted list of component names from `source-aura-event-mapping.md`. Where a component is both source and handler, annotate `(bidirectional)` once in the Handler list. If no source found in main package, write: `* None in main package — may be fired from external implementing components.`

  2. `#### Handler Component` — bulleted list. If no in-package handler, write: `* None in main package — consumed by external implementing components.`

  3. `#### Event Name (for handler)` — followed by a fenced `xml` code block with an `aura:handler` snippet:
     ```xml
     <aura:handler event="RDraw:EVT_Name" action="{!c.handleEventName}" />
     ```
     Use the exact event name from source (matches filename stem). Handler method name is `handle<PascalCasedEventName>` (e.g., `EVT_Renderer_Mesh_Selected` → `handleMeshSelected`).

  **Attribute corrections (do these in the same edit pass as the section additions):**

  - **`evt_renderer_element_added.md`:** Add row to the Parameters table:
    `| transaction | string   | optional | A transaction identifier for correlating the add operation with a later verify (see EVT_Renderer_Element_Verify) |`
    Keep the existing column structure (NAME, TYPE, DESCRIPTION). If the table has a REQUIRED column (it currently doesn't — only NAME/TYPE/DESCRIPTION), keep it consistent. Source XML shows `transaction` as required=false with `access=global`.
    (The existing table has columns NAME, TYPE, DESCRIPTION. Add the row matching those columns: `| transaction | string | A transaction identifier for correlating the add operation with a later verify (see EVT_Renderer_Element_Verify) |`.)

  - **`evt_renderer_get_hierarchy.md`:** Add row to Parameters table:
    `| transaction | string   |              | A transaction identifier for correlating the get-hierarchy call with downstream handlers |`
    The existing table has NAME / TYPE / REQUIRED / DESCRIPTION columns. Set REQUIRED to empty (no) since source has it as required=false.

  - **`evt_renderer_mesh_selected.md`:** Update REQUIRED column on existing rows to match source:
    - `name` → Yes
    - `id` → Yes
    - `uniqueId` → Yes
    - `context` → (leave blank — source required=false)

  **Preserve everything else.** Do NOT alter Usage Notes prose. Do NOT reorder sections. Do NOT touch root `SUMMARY.md` — page entries already exist under line 183 of SUMMARY.md.

  The heading-under-line escaping convention in these files uses `EVT\_Name` (escaped underscores). Preserve that convention on anything you write — but new sections use `#### Source Component`, etc., without underscores so no escaping needed.
  </action>
  <verify>
  For each of the 8 files, the file contains:
  - Original `# EVT\_*`, `### RDraw:EVT\_*`, `#### Usage Notes`, `#### Parameters` sections.
  - A new `#### Source Component` section.
  - A new `#### Handler Component` section.
  - A new `#### Event Name (for handler)` section with an xml code block containing `RDraw:EVT_`.

  Run: `grep -c "^#### Source Component$" api-documentation/3d-components-api/events-aura/evt_renderer_*.md` → returns 8 files with count 1 each (excluding the empty stub which is handled in Task 2).

  Run: `grep -c "^#### Handler Component$" api-documentation/3d-components-api/events-aura/evt_renderer_*.md` → returns 8 files with count 1 each.

  Run: `grep -c "^#### Event Name (for handler)$" api-documentation/3d-components-api/events-aura/evt_renderer_*.md` → returns 8 files with count 1 each.

  Run: `grep "transaction" api-documentation/3d-components-api/events-aura/evt_renderer_element_added.md` → at least 1 match.

  Run: `grep "transaction" api-documentation/3d-components-api/events-aura/evt_renderer_get_hierarchy.md` → at least 1 match.

  Run: `grep -A1 "| name" api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selected.md | grep -i "yes"` → at least 1 match (required=Yes cell).
  </verify>
  <done>
  All 8 pages have the three new sections in the correct position. 3 attribute-correction gaps closed (transaction on Element_Added and Get_Hierarchy; required cells on Mesh_Selected). No edits to root SUMMARY.md. Voice preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate the empty stub evt_renderer_mesh_selection_cleared.md with full content</name>
  <files>
    api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md
  </files>
  <action>
  The current file contains only:

  ```markdown
  # EVT\_Renderer\_Mesh\_Selection\_Cleared

  #### EVT\_Renderer\_Mesh\_Selection\_Cleared
  ```

  Replace the entire file with full content matching the established page structure. Source XML for this event: `access="global"`, APPLICATION type, zero attributes. The event fires when the user clicks empty canvas space (or calls `clearSelection()`) after previously selecting a mesh — it tells consumers to clear any detail panels / deselection side-effects.

  Write the file as follows:

  ```markdown
  # EVT\_Renderer\_Mesh\_Selection\_Cleared

  ### RDraw:EVT\_Renderer\_Mesh\_Selection\_Cleared

  #### Usage Notes

  This event is raised when a previously-selected mesh in a 3D scene is deselected — either because the user clicked empty canvas space, or because `clearSelection()` was called on the renderer. Handling this event allows consuming components to reset detail panels, clear related-record lookups, and revert any 3D effects applied on selection.

  #### Parameters

  This event has no parameters. Its presence alone signals the deselection.

  #### Source Component

  * [From source-aura-event-mapping.md — typically AdvancedRenderer, SimpleRenderer]

  #### Handler Component

  * [From source-aura-event-mapping.md — may be "None in main package — consumed by external implementing components."]

  #### Event Name (for handler)

  \`\`\`xml
  <aura:handler event="RDraw:EVT_Renderer_Mesh_Selection_Cleared" action="{!c.handleMeshSelectionCleared}" />
  \`\`\`

  #### Example Usage

  Component

  \`\`\`
    <aura:component implements="lightning:availableForFlowScreens,flexipage:availableForAllPageTypes,force:hasRecordId">
        <aura:handler name="selectionCleared" event="RDraw:EVT_Renderer_Mesh_Selection_Cleared"
              action="{!c.handleSelectionCleared}" />
        <RDraw:AdvancedRenderer displayContext="true" aura:id="renderer"
                              nodeIgnoreList="{!v.excludeMeshNames}" displayContextDetails="{!v.displayQuoteLineDetails}"
                              displaySidebar="{!v.showSidebar}" size="Medium" allowSelection="true"
                              recordId="{!v.selectedQuoteLineId}">
                              ...
                              </RDraw:AdvancedRenderer>
    </aura:component>
  \`\`\`

  Controller

  \`\`\`
  ({
      handleSelectionCleared: function (component, event, helper) {
          // Clear any record-detail UI and reset related state
          component.set('v.selectedLineItem', null);
          component.set('v.displayQuoteLineDetails', false);
      },
  })
  \`\`\`
  ```

  Use the Source Component and Handler Component lists from `.planning/phases/03-aura-events/source-aura-event-mapping.md`. Replace the placeholders in `[brackets]` above with actual data.

  Match the style of sibling pages exactly: escaped underscores in the top `#` and `###` headings (`EVT\_Renderer\_Mesh\_Selection\_Cleared`), bare code fences with no language tag for the Component/Controller blocks (matches `evt_renderer_loaded.md` and others), `####` for subsections.

  Do NOT touch root `SUMMARY.md` — entry for this page already exists at line 190.
  </action>
  <verify>
  `grep -c "^# EVT" api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md` → 1

  `grep -c "^#### Usage Notes$\|^#### Parameters$\|^#### Source Component$\|^#### Handler Component$\|^#### Event Name" api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md` → 5

  `wc -l api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md` → ≥30 (file is no longer a 3-line stub).

  `grep "RDraw:EVT_Renderer_Mesh_Selection_Cleared" api-documentation/3d-components-api/events-aura/evt_renderer_mesh_selection_cleared.md` → ≥2 matches (anchor heading + handler snippet + example handler).
  </verify>
  <done>
  Empty stub is now a complete event page matching the established structure: Usage Notes, Parameters (with "no parameters" explanation), Source Component, Handler Component, Event Name (for handler), Example Usage with Aura Component + Controller code blocks.
  </done>
</task>

</tasks>

<verification>
Before declaring plan 03-02 complete:

- [ ] All 9 Aura event pages under `api-documentation/3d-components-api/events-aura/` now contain Source Component, Handler Component, and Event Name (for handler) sections.
- [ ] `evt_renderer_element_added.md` parameter table includes `transaction`.
- [ ] `evt_renderer_get_hierarchy.md` parameter table includes `transaction`.
- [ ] `evt_renderer_mesh_selected.md` required cells set for name, id, uniqueId (not context).
- [ ] `evt_renderer_mesh_selection_cleared.md` is a complete page (no longer a 3-line stub).
- [ ] Root `SUMMARY.md` not modified.
- [ ] `events-aura/README.md` not modified (handled in Plan 03-05).
- [ ] No pages moved, no pages created outside the existing directory.
</verification>

<success_criteria>
- Both tasks complete.
- All verification checks pass.
- Every Source/Handler list on every page matches `source-aura-event-mapping.md` exactly.
- Every attribute in every Parameters table agrees with Phase 1 `source-lms-events.md` (no drift).
- Voice preserved — no reorganization, no tone changes, no removed content.
- Plans 03-03 and 03-04 have a reference pattern (the three-section addition) to copy for newly-created pages.
</success_criteria>

<output>
After completion, create `.planning/phases/03-aura-events/03-02-SUMMARY.md` following the standard summary template.

Frontmatter:
- `phase: 03-aura-events`, `plan: 02`, `subsystem: aura-events`
- `requires: [03-01]`, `provides: [renderer-events-enriched, aura-event-section-pattern]`, `affects: [03-03, 03-04, 03-05]`
- `tags: [docs, aura, events, renderer, gitbook]`
- `key-decisions`:
  - "Source Component / Handler Component / Event Name (for handler) sections placed between Parameters and Example Usage on every event page — mirrors Phase 2 LMS pattern adapted for Aura fire/handle semantics."
  - "Transaction attribute restored on Element_Added and Get_Hierarchy pages (was silently missing; source XML has it)."
  - "Empty stub evt_renderer_mesh_selection_cleared.md populated with full page rather than deferred — stubs are a source of confusion for external doc readers."
- `key-files`: the 9 modified files.

Body:
- Substantive one-liner naming the 9 pages enriched and the 3 attribute corrections.
- `## Accomplishments` — summarize section additions and corrections.
- `## Files Created/Modified` — list each file with one-line change description.
- `## Decisions Made` — the key decisions listed above.
- `## Issues Encountered` — any Source/Handler mapping gaps that required "None in main package" annotations.
- `## Next Step` — "Ready for 03-03-PLAN.md (document 10 undocumented Renderer events)."
</output>
